'use strict';

var TuneWikiLyricsApi = function (client) {
    return function (artist, title, language, cb) {
        client.get('/lyrics/' + artist + '/' + title, language, cb);
    };
};

module.exports = TuneWikiLyricsApi;